<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/secondary">

    <!-- Título -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/actividad_08"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/details2"
        android:fontFamily="@font/pokemon_pixel_font"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <!-- Campo para agregar número -->
    <com.google.android.material.textfield.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/n_mero_de_tel_fono"
        app:boxStrokeColor="@color/details1"
        android:layout_marginBottom="16dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etPhoneNumber"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="phone"
            android:fontFamily="@font/pokemon_pixel_font" />

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Botón para agregar número -->
    <Button
        android:id="@+id/btnAddNumber"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/agregar_n_mero"
        android:layout_marginBottom="24dp" />

    <!-- Título de la lista -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/n_meros_monitoreados"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/details2"
        android:fontFamily="@font/pokemon_pixel_font"
        android:layout_marginBottom="12dp" />

    <!-- Lista de números -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvPhoneNumbers"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/tertiary"
        android:padding="8dp" />

    <!-- Botón de testing (solo para desarrollo) -->
    <Button
        android:id="@+id/btnTest"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/simular_sms"
        android:layout_marginTop="16dp"
        android:backgroundTint="@color/fire"
        android:textColor="@color/steel" />

    <!-- Estado del monitoreo -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="16dp"
        android:background="@color/tertiary"
        android:layout_marginTop="16dp">

        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/monitoreo_activo"
            android:textSize="16sp"
            android:textColor="@color/details2"
            android:fontFamily="@font/pokemon_pixel_font" />

    </LinearLayout>

</LinearLayout>